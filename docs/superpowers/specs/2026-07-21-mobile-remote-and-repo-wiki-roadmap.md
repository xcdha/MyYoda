# 远期能力路线图占位：手机远程编程 · Repo Wiki

日期：2026-07-21  
状态：占位（只锁问题 / 边界 / 不做；**不实现**）  
前置：项目中心 Hub + Agent 专家 + Kanban TaskRunner 专家注入（已落地或进行中）  
关系：与当前 `feature/work-mode-kanban` / 专家线正交；开干前须单独 brainstorm + design + plan

---

## 1. 问题

### 1.1 手机端远程编程（类 Cursor Remote / 移动续跑）

1. 现网是 **Electron 桌面 + 本地优先**（`~/.luxcoder[-dev]/`）；离开工位无法看编排 live、批权限、续跑会话。  
2. 「手机当 thin client」需要：鉴权、会话/流式桥、权限/AskUser 移动 UI、与桌面单写者冲突策略——现网 IPC 模型未覆盖。  
3. 若只做只读看板/通知，价值有限；若做完整编程，范围会膨胀到远程 cwd / 终端 / 文件编辑。

### 1.2 Repo Wiki（类阿里 Qoder / 同类「仓库知识层」）

1. 项目中心已有项目壳与看板，但缺少 **仓库级、可检索、可随代码演进的知识层**（结构、模块说明、决策记录、变更摘要）。  
2. Agent / 专家 preamble 目前靠 IDENTITY/SOUL/RULES + skill slug；缺「读仓库 wiki」的一等入口时，长项目上下文仍靠对话堆。  
3. Wiki 若做成在线 DB 或强耦合云，会与「本地文件优先、可移植」冲突。

---

## 2. 决策摘要（占位锁定）

| 决策点 | 定案（占位） |
|--------|----------------|
| 文档性质 | **路线图占位**；不替代正式 design；开干前重开 brainstorm |
| 与当前迭代 | **不进入** 当前 Kanban / 专家 UX 欠账实现队列 |
| 两条能力关系 | **两条独立史诗**；可共享「项目」实体，但不共享第一版实现 |
| 存储原则（预约束） | 继续 **本地文件优先**；云同步 / 账号体系若需要，单独立项 |
| 实现顺序（预约束） | 先收口专家/看板线 → 再选一条史诗开 design；**禁止双开** |

---

## 3. 边界（In / Out）

### 3.1 手机远程编程

**可能纳入（仅作问题边界，未选型）**

- 移动端查看：Agent/Kanban 会话列表、编排条进度、流式摘要  
- 移动端交互：权限批准/拒绝、AskUser、停止/续跑  
- 桌面（或本机 Agent 宿主）保持执行面；手机不跑 SDK binary  

**明确不做（本占位生命周期内）**

- 不在本文件拍板：推送通道（APNs/FCM）、自建 relay、还是 SSH/隧道  
- 不做完整移动 IDE（多文件编辑器、本地终端模拟器一等公民）  
- 不做「无桌面宿主」的纯云端 Agent 执行（与本地优先冲突，需另立产品决策）  
- 不改现有 Electron IPC 四件套为默认移动协议（须另定 API / auth）

### 3.2 Repo Wiki

**可能纳入（仅作问题边界，未选型）**

- 项目下 `wiki/`（或等价）Markdown / 索引；项目中心入口  
- Agent / 专家可引用 wiki slug 或路径（类 skill 引用，非整库内联）  
- 可选：由任务/会话触发的「更新 wiki 某页」工作流（仍文件落盘）  

**明确不做（本占位生命周期内）**

- 不上独立 Wiki 数据库或强制 SaaS  
- 不做与 Git hosting（GitHub Wiki / GitLab）的双向同步 MVP  
- 不做自动全仓扫描生成「永不过时」知识图谱的承诺（生成策略须另 design）  
- 不与蜂群 / Bot 绑定为前置条件  

---

## 4. 不做清单（总表）

| 项 | 原因 |
|----|------|
| 本占位直接开工实现 | 范围未选型，避免半成品协议 |
| 与 Kanban 专家线同一 PR 混入 | 正交史诗，评审与回滚成本高 |
| 先做云账号再谈本地能力 | 颠倒现网「本地优先」假设 |
| 手机端打包进当前 electron-builder 矩阵当正式产物 | 需独立 app / 分发通道决策 |
| Repo Wiki 复制 Skill 全文进每轮 prompt | token 膨胀；沿用「引用按需加载」 |

---

## 5. 与现网模块的挂钩点（仅索引，不设计）

| 能力 | 可能挂钩 | 备注 |
|------|----------|------|
| 手机远程 | Agent 会话 / 全局 IPC 监听 / 权限排队 / Kanban Conductor | 执行面仍在桌面宿主 |
| Repo Wiki | 项目中心 Hub、`ProjectConfig`、专家 skill 引用模式、TaskRunner preamble | 文件布局须对齐 `~/.luxcoder*` 或项目 `workingDirectory` |

---

## 6. 开放问题（开 brainstorm 时再问）

1. 手机端 MVP：只读监控 vs 权限+续跑 vs 轻量发消息？  
2. 多设备同时打开同一会话：单写者锁还是 OT/CRDT（预判：单写者）？  
3. Wiki 根目录：全局 `~/.luxcoder/wiki` vs 每项目 `workingDirectory/wiki` vs 二者兼有？  
4. Wiki 生成：手动编辑为主，还是允许 Agent 定期维护指定页面？  
5. 「类 Qoder」要对齐的是产品概念（仓库知识层）还是具体交互（需对照竞品再锁）？

---

## 7. 成功标准（占位文档自身）

1. 读者能区分：**当前可做的专家/看板线** vs **未立项的两条史诗**。  
2. 任意实现 PR 若声称「手机远程 / Repo Wiki」却无独立 design，可据此驳回。  
3. 本文不包含 API、UI 线框、库选型或工期承诺。

---

## 8. 修订关系

- 不取代：`2026-07-21-projects-hub-agent-experts-design.md`、`2026-07-21-expert-runtime-injection-design.md`  
- 后续：每条史诗开干时新建 `YYYY-MM-DD-<topic>-design.md`，本文仅保留索引与不做边界；冲突以新 design 为准并回写本节链接
