# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# 编译产物：myyoda CLI 二进制（build:cli 用 bun build --compile 生成，打包时现编，不进 git）
apps/electron/resources/bin/
# 编译产物：EventKit N-API bridge（build:eventkit-native 用 clang++ 生成，打包时现编，不进 git）
apps/electron/resources/eventkit/
# bun build --compile 的临时中间产物
*.bun-build

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# ignore craft-agents-oss folder
craft-agents-oss

# ignore what_are_humans_thinking folder
what_are_humans_thinking

# vendor binaries
vendor

# git worktrees (managed by proma-worktree skill)
.worktrees
# pnpm (本项目用 bun，禁止误提交 pnpm lockfile)
pnpm-lock.yaml
pnpm-workspace.yaml

# Proma 会话/工作区文档（不应入仓，见 CLAUDE.md 红线）
.context/

# superpowers brainstorming 可视化配套（本地临时 mockup，不入仓）
.superpowers/

# Claude Code local worktrees/settings (not part of this repo)
.claude/
# dashi-ppt skill 的浏览器运行时产物必须进 git（否则发布包缺 dist 无法渲染）
!apps/electron/default-skills/**/dist/
!apps/electron/default-skills/**/dist/**
!apps/electron/default-skills/**/vendor/
!apps/electron/default-skills/**/vendor/**
